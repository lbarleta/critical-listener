# Copy to `.env` and fill in values. Start the app from the repo root
# (see scripts/run_app.sh) so PYTHONPATH resolves correctly.
PYTHONPATH=.

LASTFM_API_KEY=your_lastfm_api_key_here
ANTHROPIC_API_KEY=your_anthropic_api_key_here
